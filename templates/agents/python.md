# {{PROJECT_NAME}}

{{DESCRIPTION}}

## Stack
- **Framework**: {{FRAMEWORK}}
- **Base de datos**: {{DATABASE}}
- **Testing**: pytest

## Estructura
```
src/
├── main.py         # Entrada principal
├── models/         # Modelos de datos
├── services/       # Lógica de negocio
├── api/            # Endpoints (si aplica)
└── utils/          # Utilidades
tests/
└── test_*.py       # Tests
```

## Comandos
```bash
python -m venv venv           # Crear entorno
pip install -r requirements.txt
python src/main.py            # Ejecutar
pytest                        # Tests
```

## Skills
{{SKILLS_LIST}}

### Auto-invocación
- Código Python → skill `python`
- Tests → skill `pytest`
- API endpoints → skill `fastapi` o `django`
- Commits → skill `commits`

## Contexto
Nivel: {{LEVEL}}
{{LEVEL_INSTRUCTIONS}}
